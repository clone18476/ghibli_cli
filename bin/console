#!/usr/bin/env ruby

# this console file is the executable file 

require_relative "../environment.rb"

cli_instance = CLI.new
cli_instance.start  
# cli.new.start can replace the above two lines, that works just fine
